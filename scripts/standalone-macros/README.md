# About
Macros that can be executed using assigned toolbar buttons.

# Installation
Add links to the scripts in the macros directory of FreeCAD.

```
cd ~/.local/share/FreeCAD/Macro
ln -rs path/to/git/repository/scripts/script.py
```

