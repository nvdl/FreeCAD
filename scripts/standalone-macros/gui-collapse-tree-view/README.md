# About
Collapse the tree view of objects in the GUI completely.

# Installation
Add a link to the script in the macros directory of FreeCAD.

```
cd ~/.local/share/FreeCAD/Macro
ln -rs path/to/git/repository/scripts/gui_collapse_tree_view.py
```

